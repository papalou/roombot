Simple projet for my stm32f103 (the cheap 3$ board)

Install 
	openocd

Build: 
	cd src
	make

Flash:
	Connect stm flasher
	cd src
	make write (sudo can be needed)

After the clone of the repository you need to launch "make_dev_env.sh"
script to set up the development environement.
